# Atom Components

基础组件实现位于 `components/<Name>/`。单独预览：该目录下的 `preview.html`；本层总览：`index.html`。

```sh
node scripts/build.mjs atom Button
node scripts/verify.mjs atom Button
node scripts/build.mjs atom --gallery
```

从工程根目录运行。详细目录与工作流见 [工程说明](../README.md)。
